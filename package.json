{
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build:compile": "bun build --compile --minify --sourcemap src/server.ts --outfile todo-app"
  },
  "dependencies": {
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6"
  }
}
